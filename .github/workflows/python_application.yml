name: Python Application CI

#on:
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9
        
    - name: Debug current directory and list files
      run: |
        echo "Current working directory:"
        pwd
        echo "List of files in the current directory:"
        ls -la

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Install lib-ml
      run: pip install .

    - name: Run ML tests and generate coverage report
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_BUCKET_NAME: ${{ secrets.AWS_BUCKET_NAME }}
      run: |
        pytest --cov tests

    - name: Publish model and show metrics
      run: |
        python src/define_train_model.py
        # Add commands to upload model to a cloud or save it with a public link
        # Example: aws s3 cp model.pkl s3://yourbucket/model.pkl --acl public-read
  
  stylecheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        if [ $? -ne 0 ]; then exit 1; fi
        flake8 . --count --max-complexity=10 --max-line-length=127 --statistics
        if [ $? -ne 0 ]; then exit 1; fi

    - name: Lint with pylint
      run: |
        pip install pylint
        pylint --fail-under=7 .
